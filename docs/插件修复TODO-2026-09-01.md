# 插件修复 Todo（2026-09-01）

本文针对社区版仍注册的历史插件进行在线盘点，按影视网盘资源优先、修复风险和维护成本排序。已确认下线、停放或必须浏览器登录的站点不重新加入默认插件列表。

## 已完成

- [x] `yunsou`（A 类）：旧域名/JSON 接口失效，迁移至 `https://wpys.cc` 的 HTML 搜索和分页，解析夸克、百度、迅雷、阿里、UC 链接。
- [x] `djgou`（A 类）：适配新 Z-Blog 搜索模板、详情页网盘区域，并兼容 BTWAF 跳转。
- [x] `hdmoli`（A 类）：从 `.pro` 切换到 `.com`，修复查询参数被重定向丢失的问题；验证夸克和百度链接。
- [x] `panlian`（A 类）：按新版站点流程预建 `PHPSESSID` 后登录；支持 token 解析接口、跳转页兜底和 123 网盘分享地址归一化。
- [x] `meitizy`（A 类）：适配前后端分离后的 `apis.451024.xyz/api/media/search`，补充 Origin/Referer、链接校验和 URL 提取码解析；“仙逆”已返回夸克、百度、迅雷结果。
- [x] `yulinshufa`（A 类）：复测当前 GBK 搜索页和详情页协议，已能提取“仙逆”详情中的迅雷分享链接，无需代码改动。

## 第一批：低风险，待环境恢复后验证

- [x] `pansearch`（A 类，已默认启用）：已完成动态 buildId、请求并发、HTML 解析和真实链接校验修复；源站对部分 IP 有 403/IP 风控，部署在受限网络时需配置 `PROXY`。

## 第二批：中等改造

- [ ] `ahhhhfs`、`haisou`、`jupansou`、`cyg`：当前请求返回异常，需继续确认新接口或域名。

## 困难或暂不修复

- `dyyj`、`leijing`、`alupan`、`ash`、`bixin`、`miaoso`、`wuji`、`yiove`、`yuhuage`、`aikanzy`：Cloudflare/403/滑动验证，插件无法在标准 HTTP 客户端中稳定工作。
- `discourse`：受 Linux.do 登录和访问策略限制。
- `panzun`：已确认新 API 为 `/2k`，但影视主题正文和网盘链接由 reply2see 登录/回复后显示，匿名插件无法获得真实链接，暂不改造登录流程。
- `xuexizhinan`：自签名证书，不绕过 TLS 校验。
- `mikuclub`：apex 域名强制跳转到 `www`，而 `www` 证书域名不匹配；不跳过 TLS 校验。
- `susu`：搜索列表仍可用，但网盘链接接口命中 Cloudflare Challenge，现有插件返回的 20 条均无链接，不能恢复。

## 排除（下线、停放、重复或非影视优先）

- DNS/主机失效：`erxiao`、`jikepan`、`labi`、`qingying`、`xiaoji`、`ypfxw`、`zhizhen`、`cldi`、`clmao`、`clxiong`、`huban`、`hdr4k`、`javdb`、`panta`、`shandian`、`daishudj`。
- Cloudflare Managed Challenge：`diduan`（`https://ddys.io`，服务端请求被拦截，暂不绕过验证）。
- 接口/域名已失效：`duanjuw`（410）、`qupansou`（跳转停放页）、`sdso`（ParkLogic）、`pianku`（停放域名）。
- 站点已关闭或停放：`kkmao`（Farewell 页面）、`xinjuc`（lander 停放页）。
- 备用域名均不可用：`qiwei`（超时、520 或 API 404）。
- 被替代或无实现：`xys`（与 `yunso` 同源）、`pioz`（无 Go 实现）。
- 重复实现：`xdyh` 当前前端调用 `https://psapi.138039.xyz/pansou-api/search`，本质上是 PanSou 聚合 API 的前端包装，并额外依赖 Shield Guard 浏览器 WASM 签名；不再单独接入。
- 不符合安全基线：`panyq` 依赖 `InsecureSkipVerify`，不恢复。
- 非影视优先：`nsgame`。

## 后置任务

- [ ] `rrbt`（人人 BT，最低优先级）：`https://www.rrbts.org/`，按用户要求在其他插件处理完后再实现，默认不启用。

## 默认启用策略

修复完成并通过真实标题探针后，才同步到主版、`pansou-web`、`lpansou`、`lpansou-web` 的 `ENABLED_PLUGINS`。`pansearch` 已同步启用，但受源站 IP 风控影响，建议在必要时配置 `PROXY`。社区版的 `AUTH_ENABLED`、Linux.do OAuth、水印和认证配置保持原样。
